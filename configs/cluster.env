# Cluster connection settings — edit before running benchmarks
BASE_URL=https://ray-cluster-head.ml-XXXX.example.cloudera.site

# vLLM engine
VLLM_ROUTE=/qwen-2b
VLLM_MODEL=/home/cdsw/models/Qwen3.5-2B

# YOLO engine
YOLO_ROUTE=/yolo

# MCP engine
MCP_ROUTE=/weather-mcp

# SSL (set to false for self-signed certs on CML)
VERIFY_SSL=false
